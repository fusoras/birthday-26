---
import { ROUTES } from "@/config.ts"
import { CRAFT_P_1, CRAFT_P_2, CRAFT_P_3, CRAFT_MODAL_1 } from "astro:env/client"

import Layout from "@/layouts/Layout.astro"
import Body from "@/layouts/Body.astro"
import MineBook from "@/components/ui/Book.astro"
import AllButtonNav from "@/components/AllButtonNav.astro"
import ButtonWelcome from "@/components/ButtonWelcome.svelte"
import ButtonModal from "@/components/ButtonModal.astro"
import Light from "@/components/ui/Light.astro"
import ButtonCraft from "@/components/ui/ButtonCraft.astro"
---

<Layout>
    <link rel="preload" href={`${ROUTES.ROOT}/imgs/ui/button-inside.avif`} as="image" type="image/avif" slot="head" />
    <link rel="preload" href={`${ROUTES.ROOT}/imgs/ui/button-border.png`} as="image" type="image/png" slot="head" />
    <link rel="preload" href={`${ROUTES.ROOT}/imgs/ui/button-inside-hover.avif`} as="image" type="image/avif" slot="head" />
    <link rel="preload" href={`${ROUTES.ROOT}/imgs/ui/button-border-hover.png`} as="image" type="image/png" slot="head" />
    <link rel="preload" href={`${ROUTES.ROOT}/imgs/ui/button-inside-active.avif`} as="image" type="image/avif" slot="head" />
    <link rel="preload" href={`${ROUTES.ROOT}/imgs/ui/button-border-active.png`} as="image" type="image/png" slot="head" />
    
    <div class="hidden">
		<ButtonWelcome client:load src="" transition:name="media-player" transition:persist />
	</div>
    <Body gap="gap-8">
        <section class="flex justify-center gap-2">
            <ButtonModal PopoverTarget="modal-page-1" gradient1="#ff9a9e" gradient2="#fad0c4" gradient3="#fad0c4">
                    {CRAFT_MODAL_1}
                <Light scale="0.8"/>
                <button slot="close" popovertarget="modal-page-1" popovertargetaction="hide">
                   <ButtonCraft>
                        Cerrar
                   </ButtonCraft>
                </button>
            </ButtonModal>
        </section>
        <MineBook>
            <p class="p-craft-content">{CRAFT_P_1}</p>
            <p class="p-craft-content">{CRAFT_P_2}</p>
            <p class="p-craft-content">{CRAFT_P_3}</p>
        </MineBook>
        <AllButtonNav src1={ROUTES.MUSIC} src2={ROUTES.LETTER} />
    </Body>
</Layout>
<style>
    .p-craft-content{
        margin-block: 1lh;

        &:first-of-type {
            margin-top: 0;
        }
        &:last-of-type {
            margin-bottom: 0;
        }
    }
</style>